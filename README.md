# 月相羅針 計算PoC v0.1

春華プロジェクトの「月相羅針 Web計算サービス」を技術検証するための、小規模な Flask Web アプリです。

## 重要

この PoC は、**月相羅針の正式占術仕様を確定するものではありません。**

現在の P01〜P08 は、360°を45°ずつ8等分した技術確認用の仮分類です。正式な月相羅針の ID・名称・角度範囲・境界条件・説明文・特徴・進み方が確定したら、`phase_classifier.py` の定義を差し替える前提です。

## 実装範囲

1. 生年月日・出生時間・出生地を入力
2. PoC v0.1では出生地を日本国内として `Asia/Tokyo` に解決
3. 出生時刻を UTC へ変換
4. pyswisseph で地心・トロピカルの太陽黄経・月黄経を計算
5. `(moon_longitude - sun_longitude) % 360.0` で角度差を算出
6. 技術確認用の仮8分類を判定
7. ブラウザ画面へ結果を表示

## 必要環境

- Python 3.9 以上を推奨
- pip

## インストール

プロジェクトフォルダへ移動して実行します。

```bash
pip install -r requirements.txt
```

## 起動

```bash
python app.py
```

ブラウザで次を開きます。

```text
http://127.0.0.1:5000
```

## GitHub Codespaces での起動

Codespaces の Terminal で次を実行します。

```bash
pip install -r requirements.txt
python app.py
```

Flask は `0.0.0.0:5000` で待ち受けます。Codespaces の **PORTS** タブで **5000** 番ポートを見つけ、**Open in Browser** を選択してください。

## 正解確認用テストデータ

- 生年月日：1964-09-03
- 出生時間：11:23
- 出生地：兵庫県小野市
- タイムゾーン：Asia/Tokyo

期待値（概算）：

- UTC：1964-09-03 02:23:00 UTC
- 太陽黄経：約 160.60945188°
- 月黄経：約 119.86709682°
- 角度差：約 319.25764494°
- 仮8分類：P08 / 仮分類8

## テスト

```bash
python -m unittest discover -s tests -v
```

テストでは、上記の基準データと次の仮分類境界を確認します。

- 0° → P01
- 44.9999° → P01
- 45° → P02
- 314.9999° → P07
- 315° → P08
- 359.9999° → P08
- 360° → 0°として P01

## ファイル構成

```text
gessou_rashin_poc/
├── app.py
├── astronomy.py
├── phase_classifier.py
├── location_master.py
├── requirements.txt
├── README.md
├── templates/
│   └── index.html
├── static/
│   └── style.css
└── tests/
    └── test_core.py
```

## モジュール分離方針

- `astronomy.py`：現地時刻→UTC→ユリウス日→太陽・月黄経→角度差
- `phase_classifier.py`：月相羅針の分類定義・判定
- `location_master.py`：出生地→タイムゾーン（将来は緯度・経度・歴史的タイムゾーンへ拡張）
- `app.py`：入力、検証、各モジュール呼び出し、結果・エラー表示

これにより、正式な月相羅針仕様が決まった際に、天体計算ロジックを作り直さず分類定義を更新できます。

## 天体暦について

コードは `pyswisseph` に対して Swiss Ephemeris モードを要求します。外部の Swiss Ephemeris 天体暦データファイルが環境に存在しない場合、ライブラリが Moshier モードへフォールバックすることがあります。画面には実際に使用されたモードを表示します。

PoCの再現性をより厳密に固定する段階では、使用する天体暦データファイル、バージョン、配置方法まで固定して管理してください。

## ライセンス上の注意

Swiss Ephemeris はライセンス条件を持つソフトウェアです。`pyswisseph` を利用した本PoCを一般公開・配布・商用利用へ進める場合は、**Swiss Ephemeris および pyswisseph の最新の公式ライセンス条件を、実施時点で必ず確認してください。**

特に、公開形態、ソースコードの扱い、商用利用の有無によって必要な対応が変わり得るため、正式提供前の確認事項とします。

## PoC v0.1 で実装していないもの

会員登録、ログイン、決済、AI機能、AI春華接続、個人鑑定、カード機能、履歴保存、SNS共有、高度デザイン、本格的な出生地検索、地図連携、外国出生地対応、データベース、管理画面は対象外です。
